[uwsgi]
# Doesn't seem to work with more than one socket mode
# Choose one at a time
http = :9090
#fastcgi-socket = /uwsgi.d/fast-uwsgi
wsgi-file = /uwsgi.d/uwsgi_server.py
master = true
processes = 4
threads = 2
stats = 0.0.0.0:9191
uid = uwsgi
gid = uwsgi
